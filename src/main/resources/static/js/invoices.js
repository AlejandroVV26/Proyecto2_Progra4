function invoices() {
    return `
        <header>
            <h1>Declarar</h1>
        </header>
        <div>
            ${renderMenu('proveedor')}
        </div>
        <section class="content">
            <!-- Agrega el formulario y la lógica para declarar facturas aquí -->
        </section>
    `;
}